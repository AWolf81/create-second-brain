# Agent instructions

Before writing anything into this vault, read `WHERE-THINGS-LIVE.md`. It says which
system is authoritative for what, and whether a given fact belongs here at all.

@WHERE-THINGS-LIVE.md

---

⚠ COG's `cog-update.sh` treats `CLAUDE.md` as a framework file and will offer to replace
it. Decline, or re-add the import line above afterwards. `./scripts/doctor.sh` checks
that it is still there, because a silently dropped import is indistinguishable from
having no conventions at all.
